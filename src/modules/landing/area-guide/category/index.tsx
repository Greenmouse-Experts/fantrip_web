import { FC } from "react"
import { PlaceItem } from "@/lib/contracts/place"

interface Props{
    data: PlaceItem[]
}
const ViewCategoryRecommendations:FC<Props> = ({}) => {
  return (
    <div>ViewCategoryRecommendations</div>
  )
}

export default ViewCategoryRecommendations